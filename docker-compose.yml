services:
  api:
    build: .
    image: phansora-api
    env_file: .env
    ports:
      - "${PORT:-8000}:8000"
    volumes:
      - ./data:/app/data
      - media:/app/book_alchemy
    depends_on:
      - db
    restart: unless-stopped

  worker:
    image: phansora-api
    build: .
    command: ["python", "-m", "phansora.products.spokenverse.worker"]
    env_file: .env
    volumes:
      - ./data:/app/data
      - media:/app/book_alchemy
    depends_on:
      - db
    restart: unless-stopped

  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: ${DB_USER:-phansora}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-phansora}
      POSTGRES_DB: ${DB_NAME:-phansora}
    ports:
      - "${DB_PORT:-5432}:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    restart: unless-stopped

volumes:
  pgdata:
  media:
