"""Image Tools (planned product — scaffold).

Reserved package for the upcoming image-processing product. Follow the same
layout as the existing products: expose a FastAPI ``app`` (or router), keep
prompts/models/config local, and use ``phanoris.shared`` for AI clients,
storage and billing.
"""
